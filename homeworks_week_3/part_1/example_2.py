"""Comparaciones con python en Assert"""

# Todas estas funciones deben de estar previamente definidas para que se pueda ejecutar si no no podra correr el program

assert temperature < 0
assert len(given_name) > 0
assert balance == 0
assert school_year != "senior"
